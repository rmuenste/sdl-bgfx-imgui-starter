# AGENTS

Notes for automation and future work in this repo:

- Prefer the superbuild path (`-DSUPERBUILD=ON`) for third-party deps.
- Use `BGFX_BUILD_EXAMPLES=ON` when you need bgfx example binaries.
- Example binaries land under the bgfx sub-build tree, not the top-level build.
